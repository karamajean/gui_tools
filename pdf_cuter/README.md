# PDF 頁面擷取工具

這是一個使用 C# WinForms 開發的 PDF 頁面擷取工具，可以從 PDF 檔案中提取指定的頁面並保存為新的 PDF 檔案。

## 功能特色

- 🎯 **簡單易用**：直觀的圖形用戶界面
- 📄 **靈活頁面選擇**：支援多種頁面範圍格式
- 🔍 **自動頁數檢測**：載入 PDF 後自動顯示總頁數
- 💾 **批次處理**：支援多頁面範圍的批次擷取
- 🛡️ **錯誤處理**：完善的錯誤提示和驗證

## 頁面範圍格式

支援以下格式的頁面範圍輸入：

- **單一頁面**：`5` (擷取第5頁)
- **連續範圍**：`1-10` (擷取第1到第10頁)
- **多個範圍**：`1-5,8,10-15` (擷取第1-5頁、第8頁、第10-15頁)
- **混合格式**：`1,3-5,7,9-12` (擷取第1頁、第3-5頁、第7頁、第9-12頁)

## 系統需求

- Windows 10 或更新版本
- .NET 6.0 或更新版本
- Visual Studio 2022 (用於開發)

## 安裝與使用

### 方法一：使用 Visual Studio

1. 開啟 Visual Studio 2022
2. 選擇「開啟專案或解決方案」
3. 選擇 `PDFCutter.csproj` 檔案
4. 按 F5 或點擊「開始偵錯」執行

### 方法二：使用命令列

```bash
# 還原 NuGet 套件
dotnet restore

# 建置專案
dotnet build

# 執行應用程式
dotnet run
```

## 使用步驟

1. **選擇輸入檔案**：點擊「選擇檔案」按鈕，選擇要處理的 PDF 檔案
2. **設定輸出位置**：點擊「選擇位置」按鈕，設定新 PDF 檔案的儲存位置
3. **輸入頁面範圍**：在頁面範圍欄位中輸入要擷取的頁面
4. **執行擷取**：點擊「擷取頁面」按鈕開始處理
5. **完成**：等待處理完成，會顯示成功訊息

## 技術架構

- **開發語言**：C# 8.0+
- **UI 框架**：Windows Forms (.NET 6)
- **PDF 處理**：iTextSharp 5.5.13.3
- **目標框架**：.NET 6.0 Windows

## 專案結構

```
PDFCutter/
├── PDFCutter.csproj      # 專案檔案
├── Program.cs            # 程式入口點
├── Form1.cs              # 主窗體邏輯
├── Form1.Designer.cs     # 窗體設計器
└── README.md             # 說明文件
```

## 注意事項

- 確保輸入的 PDF 檔案沒有密碼保護
- 頁面範圍不能超過 PDF 的實際頁數
- 輸出檔案路徑不能與輸入檔案相同
- 建議在處理大型 PDF 檔案時耐心等待

## 授權

此專案僅供學習和個人使用。

## 問題回報

如果遇到任何問題或需要新功能，請在專案中提出 Issue。 